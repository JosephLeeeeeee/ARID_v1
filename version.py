import torch
import torchvision
import cv2

print("pytorch version is", torch.__version__)

print("torchvision version is", torchvision.__version__)

print("cuda version is", torch.cuda_version)

print("open cv version is",cv2.__version__)
